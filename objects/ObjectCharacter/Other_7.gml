/// @description Insert description here

if (!is_undefined(attackScript)) {
	attackScript = undefined;
	canAttack = true;
	moveScript = basic_movement;
}

if (!is_undefined(rotate)) rotate = undefined;